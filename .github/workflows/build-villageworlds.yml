name: Build Village Worlds
on:
  push:
    branches: [disabled-during-expansion]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Expansion branch updates are temporarily batched"
